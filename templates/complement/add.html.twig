{% extends 'admin/admin.html.twig' %}

{% block title %}Hello BurgerController!
{% endblock %}

{% block body %}
{{ parent() }}

	<div class="container mt-5  justify-content-center "style="width: 50rem;">
		<div class="card text-left">
			<img class="card-img-top" src="holder.js/100px180/" alt="">
			<div class="card-body">
				<h4 class="card-title text-center">Ajouter Complement</h4>
				{{form_start(form)}}
				<div class="form-group">
					<label for="exampleInputEmail1">Nom Complement</label>
					{{form_widget(form.nom)}}
					<small id="emailHelp" class="form-text text-danger">{{form_errors(form.nom)}}</small>
				</div>
                <div class="form-group">
					<label for="exampleInputEmail1">Prix Complement</label>
					{{form_widget(form.prix)}}
					<small id="emailHelp" class="form-text text-danger">{{form_errors(form.prix)}}</small>
				</div>
                <div class="form-group">
					<label for="exampleInputEmail1">Image Complement</label>
					{{form_widget(form.image)}}
					<small id="emailHelp" class="form-text text-danger"></small>
				</div>
					{{form_widget(form.valider)}}

				{{form_end(form)}}

			</div>
		</div>
	</div>

{% endblock %}
